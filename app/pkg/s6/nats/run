#!/command/with-contenv sh
# Start NATS server if enabled

if [ "${NATS_ENABLE}" = "true" ]; then
    echo "Starting NATS server..."
    exec nats-server -c /opt/streamops/app/pkg/nats.conf
else
    echo "NATS disabled, sleeping..."
    exec sleep infinity
fi